<!DOCTYPE node PUBLIC
	"-//freedesktop//DTD D-BUS Object Introspection 1.0//EN"
	"http://www.freedesktop.org/standards/dbus/1.0/introspect.dtd">
<node xmlns:doc="http://www.freedesktop.org/dbus/1.0/doc.dtd">
	<interface name="net.easimer.swaybar.badges.BadgeGroup1">
		<!--
			Create:
			@badge: Returned badge object of type
			#net.easimer.swaybar.badges.Badge.

			Creates a new badge.

			Implementations may return with error
			#net.easimer.swaybar.badges.ENOSPC if they can't create
			a new badge.
		-->
		<method name="Create">
			<arg name="badge" type="o" direction="out" />
		</method>
		<!--
			Destroy:
			@badge: The badge object that needs to be destroyed
			(#net.easimer.swaybar.badges.Badge).

			Destroys an existing badge.

			Implementations may return with error
            #net.easimer.swaybar.badges.ENOENT if the specified badge does not
            exist or is not a Badge object.
		-->
		<method name="Create">
			<arg name="badge" type="o" direction="out" />
		</method>
	</interface>
</node>
